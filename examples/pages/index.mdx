import Showcase from "../components/showcase"
import { LinePrimitive, PointPrimitive, FacePrimitive, ComponentType } from "co-3gen"
import { Matrix4, Shape, Vector2 } from "three"
import { test0, test1, test2, test3, test4, test5, test6 } from "../components/test"

# 3D Operator Examples

a simple Face-Primitive / a building lot

<Showcase primitive={test0} />

advanced structure using `connect` & `transform` operator

<Showcase primitive={test1} />

multiple `boolean3d` operators  
_The resulting geometry is still 100% clean_

<Showcase primitive={test2} />

`boolean2d` & `connect` operator

<Showcase primitive={test3} />

`sample2d` & `connect` operator  
_the normal of the face is preserved_

<Showcase primitive={test4} />

<Showcase primitive={test5} />

<Showcase primitive={test6} />
